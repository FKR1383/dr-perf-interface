# Own-Property Name Fast Path

Instrument `v8::internal::Runtime_ObjectGetOwnPropertyNamesTryFast` with the `v8-interpreter-038` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
