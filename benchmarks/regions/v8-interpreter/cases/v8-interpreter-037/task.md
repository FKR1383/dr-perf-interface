# Object Key Enumeration

Instrument `v8::internal::Runtime_ObjectKeys` with the `v8-interpreter-037` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
