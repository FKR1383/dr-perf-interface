# Dynamic Binding Deletion

Instrument `v8::internal::Runtime_DeleteLookupSlot` with the `v8-interpreter-009` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
