# Iteration Body Generation

Instrument `v8::internal::interpreter::BytecodeGenerator::VisitIterationBody` with the `v8-interpreter-020` RAII region marker. The selected phase is Ignition bytecode generation during JavaScript compilation. Use the bounded workload described in `case.json`; its command remains unverified.
