# Property Existence Lookup

Instrument `v8::internal::Runtime_HasProperty` with the `v8-interpreter-049` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
