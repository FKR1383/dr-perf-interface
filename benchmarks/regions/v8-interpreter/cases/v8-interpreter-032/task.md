# String Builder Concatenation

Instrument `v8::internal::Runtime_StringBuilderConcat` with the `v8-interpreter-032` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
