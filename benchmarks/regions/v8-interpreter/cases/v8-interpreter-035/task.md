# String Quote Escaping

Instrument `v8::internal::Runtime_StringEscapeQuotes` with the `v8-interpreter-035` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
