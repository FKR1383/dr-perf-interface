# Array Inclusion Search

Instrument `v8::internal::Runtime_ArrayIncludes_Slow` with the `v8-interpreter-028` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
