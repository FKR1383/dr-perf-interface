# Array Element Growth

Instrument `v8::internal::Runtime_GrowArrayElements` with the `v8-interpreter-027` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
