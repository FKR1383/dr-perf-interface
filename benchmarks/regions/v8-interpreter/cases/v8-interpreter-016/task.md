# Async Function Generation

Instrument `v8::internal::interpreter::BytecodeGenerator::GenerateAsyncFunctionBody` with the `v8-interpreter-016` RAII region marker. The selected phase is Ignition bytecode generation during JavaScript compilation. Use the bounded workload described in `case.json`; its command remains unverified.
