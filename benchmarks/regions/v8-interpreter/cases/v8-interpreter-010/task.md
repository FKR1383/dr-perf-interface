# Dynamic Binding Load

Instrument `v8::internal::{anonymous}::LoadLookupSlot` with the `v8-interpreter-010` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
