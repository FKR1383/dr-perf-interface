# Own-Property Key Collection

Instrument `v8::internal::Runtime_GetOwnPropertyKeys` with the `v8-interpreter-050` RAII region marker. The selected phase is JavaScript execution. Use the bounded workload described in `case.json`; its command remains unverified.
