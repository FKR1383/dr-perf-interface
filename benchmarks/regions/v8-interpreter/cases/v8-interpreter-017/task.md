# Variable Declaration Generation

Instrument `v8::internal::interpreter::BytecodeGenerator::VisitVariableDeclaration` with the `v8-interpreter-017` RAII region marker. The selected phase is Ignition bytecode generation during JavaScript compilation. Use the bounded workload described in `case.json`; its command remains unverified.
