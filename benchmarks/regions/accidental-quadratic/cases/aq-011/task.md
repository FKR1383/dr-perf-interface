# Phrase tokenization

Instrument the body of `get_phrase` in `Lib/email/_header_value_parser.py` as region `aq-011`. Keep the source behavior unchanged.

Bounded trigger: Parse a long phrase-like header value.
